import { FiltroEmpresaPipe } from './filtro-empresa.pipe';

describe('FiltroEmpresaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEmpresaPipe();
    expect(pipe).toBeTruthy();
  });
});
