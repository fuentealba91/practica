import { FiltroApePipe } from './filtro-ape.pipe';

describe('FiltroApePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroApePipe();
    expect(pipe).toBeTruthy();
  });
});
